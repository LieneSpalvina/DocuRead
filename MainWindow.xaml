<Window
    x:Class="DocumentReaderApp.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Document Reader"
    Width="1024"
    Height="768"
    MinWidth="800"
    MinHeight="600"
    WindowStartupLocation="CenterScreen">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!--  Application Title  -->
        <TextBlock
            Grid.Row="0"
            Margin="0,30,0,30"
            HorizontalAlignment="Center"
            FontSize="30"
            Text="Handwritten Document Reader Management System"
            TextAlignment="Center"
            TextWrapping="Wrap" />

        <!--  Sign Up Button  -->
        <Button
            Name="btnSignUp"
            Grid.Row="1"
            Width="250"
            Height="60"
            Margin="0,10,0,10"
            HorizontalAlignment="Center"
            Click="SignUpButton_Click"
            Content="Sign Up"
            FontSize="25" />

        <!--  OR Text  -->
        <TextBlock
            Grid.Row="2"
            Margin="0,10,0,10"
            HorizontalAlignment="Center"
            FontSize="25"
            Text="or"
            TextAlignment="Center" />

        <!--  Log In Button  -->
        <Button
            Name="btnLogIn"
            Grid.Row="3"
            Width="250"
            Height="60"
            Margin="0,10,0,10"
            HorizontalAlignment="Center"
            Click="LoginButton_Click"
            Content="Log In"
            FontSize="25" />

    </Grid>
</Window>
